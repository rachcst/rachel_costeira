---
import { navLinks } from "../data/data.ts"
---
<div class="navContainer">
    <ul class="navLinks">
      {navLinks.map((site) => (
        <li class="navLink">
          <a id="navLink" href={site.url} >
            {site.name}
          </a>
        </li>
      ))}
    </ul>
</div>
