---
import { navLinks } from "../data/data.ts"
---
<nav>
    <ul>
      {navLinks.map((site) => (
        <li>
          <a href={site.url} >
            {site.name}
          </a>
        </li>
      ))}
    </ul>
  </nav>
